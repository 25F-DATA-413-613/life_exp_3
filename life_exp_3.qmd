---
title: "Life Exp Gapminder"
format: html
---

# Gapminder analysis

Here is a wonderful analysis of gapminder data!
I was on main!
now I am on testname
I am still there with new edits



```{r}
library(tidyverse)
library(gapminder)
```
```{r}
library(tidyverse)
gapminder::gapminder_unfiltered  |> 
  group_by(year) |>
  filter(lifeExp == max(lifeExp)) |>
  ungroup() |>
  select(year, country, lifeExp) |>
  arrange(year) ->
  sumdat
```

```{r}
sumdat |> 
  ggplot(aes(x = year, y = lifeExp)) +
  geom_point(aes(color = country)) +
  geom_point(pch = 1) +
  theme_bw() +
  geom_smooth( se = FALSE) +
  xlab("Year") +
  ylab("Maximum Life Expectancy") +
  scale_color_discrete(name = "Country")
```

